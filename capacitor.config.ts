import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dcit.studentprofile',
  appName: 'Student Profile Viewer',
  webDir: 'www'
};

export default config;
